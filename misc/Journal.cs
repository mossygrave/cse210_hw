class Program 
{

    class Journal
    {
        // menu 

        //display all the entries 
        bool exit = false
        
        while (exit == false)
        {
            Console.WriteLine("Menu");
            console.WriteLine("");
            Console.WriteLine("New Entry");
            Console.WriteLine("All Entries");
            Console.WriteLine("Save Entry");
            Console.WriteLine("Load Journal");
            Console.WriteLine("Quit")
            Console.WriteLine("What would you like to do?")
            string action = Console.ReadLine();

            // if statements with each of the menu options
            if (action == "Quit")
            {

            }
            else if (action == "New Entry")
            {

            }
            else if (action == "All Entries")
            {

            }
            else if (action == "Save Entry")
            {

            }
            else if ("Load Journal")
            {
                
            }
        }
        
        
        class PromptGenerator
        {
            // random number generator for prompts 
            // list of prompts 
            List<int> prompts = new List<int>();
            static string GeneratePrompt()
            {
                //generate random number 
                //return the prompt related to the number
                return ""
            }
        }
        class Entries
        {
            // access an individual entry
            // hold the list of entries
            // has a variable for date, prompt text, and entry text

        }
    }

}